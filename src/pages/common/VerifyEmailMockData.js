// Mock data for VerifyEmail page component

// Mock root props for testing the verification page
export const mockRootProps = {
  // Email address to display (passed via navigation state or URL params)
  email: "user@example.com",
  
  // User type for API calls (passed via navigation state or URL params)
  userType: "buyer" // or "seller" or "delivery"
};