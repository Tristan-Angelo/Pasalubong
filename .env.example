# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB installation:
MONGODB_URI=mongodb://localhost:27017/pasalubong

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/pasalubong?retryWrites=true&w=majority

# Frontend URL (for email verification links)
FRONTEND_URL=http://localhost:5173

# Email Configuration (Optional - for sending verification emails)
# If not configured, verification links will be logged to console
# 
# For Gmail:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate an "App Password" at https://myaccount.google.com/apppasswords
# 3. Use your Gmail address and the app password below
#
# For other providers, use their SMTP settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Alternative Email Services:
# 
# SendGrid:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key
#
# Mailgun:
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_USER=your-mailgun-username
# EMAIL_PASSWORD=your-mailgun-password

# Instructions:
# 1. Copy this file to .env
# 2. Update MONGODB_URI with your MongoDB connection string
# 3. For local development, make sure MongoDB is running on your machine
# 4. For production, use MongoDB Atlas or another cloud provider
# 5. (Optional) Configure email settings to send verification emails
#    - Without email config, verification links will be shown in console